// # Prefix Hierarchy

// Given a list of names, determine the number of names in that list for which a given query string is a prefix.

// For example, given the list `names = [jackson, jacques, jack]`, the complete query string `jack` is a prefix of
// <span style="color:green">jack</span>son but not of
// <span style="color:green">jac</span>
// <span style="color:red">q</span>ues or
// <span style="color:red">jack</span>.

// The prefix cannot contain the entire name string.

// **Function Description**

// Complete the function `findCompletePrefixes` in the editor below. The function must return an array of integers that each denotes the number of names strings for which a query string is a prefix.

// `findCompletePrefixes` has the following parameter(s):

// - names[names[0],...names[n-1]]: an array of name strings query[query[0]
// - query[n-1]]: an array of query strings

export function findCompletePrefixes(names: string[], query: string[]): number {
  return 0;
}
